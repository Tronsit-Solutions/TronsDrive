#global-loader.hidden{ style: "position: fixed; inset: 0; display: flex; align-items: center; justify-content: center; backdrop-filter: blur(4px); background: rgba(10,15,30,0.4); z-index: 9999;" }
  .animate-pop-in{ style: "background: var(--td-surface); padding: 2rem; border-radius: 1.5rem; box-shadow: 0 25px 60px rgba(0,0,0,0.25); border: 1px solid var(--td-border); display: flex; flex-direction: column; align-items: center; gap: 1rem;" }
    .relative{ style: "width: 4rem; height: 4rem;" }
      .absolute.inset-0.rounded-full{ style: "border: 4px solid rgba(249,115,22,0.15);" }
      .absolute.inset-0.rounded-full.animate-spin{ style: "border: 4px solid transparent; border-top-color: #F97316;" }
    .flex.flex-col.items-center
      %p{ style: "font-size: 1.1rem; font-weight: 800; color: var(--td-text); letter-spacing: -0.02em;" }
        Processing...
      %p{ style: "font-size: 0.8rem; color: var(--td-muted);" }
        Please wait while we handle your request
